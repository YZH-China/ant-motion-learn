import React, { Component } from 'react';
import TweenOne from 'rc-tween-one';

class TimeAxis extends Component {
  constructor() {
    super(...arguments);
    this.displayName="TimeAxis";
  }

  render() {
    return (
      <div>
        this is TimeAxis page
      </div>
    );
  }
}

export default TimeAxis;